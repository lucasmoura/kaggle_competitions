class StackingTransform:

    @staticmethod
    def transform(values):
        return values

    @staticmethod
    def revert(values):
        return values

    @staticmethod
    def prediction_transform(values):
        return values
